# Tools

Use the OpenClaw gateway tools provided by the Nix-managed runtime.

Secrets are mounted read-only from `/run/secrets`. Treat missing secret files as disabled integrations, not as a reason to invent credentials.
